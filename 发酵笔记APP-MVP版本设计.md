# 发酵笔记APP - MVP版本设计文档

## 一、产品概述

### 1.1 产品定位
发酵笔记是一款专注于家庭发酵过程管理的轻量级记录工具，帮助发酵爱好者轻松追踪多个发酵项目的状态变化。

### 1.2 目标用户
- 家庭发酵爱好者（康普茶、泡菜、酸奶）
- 烘焙爱好者（酵母、酸面团管理）
- 发酵初学者

### 1.3 核心价值
- **简单记录**：快速记录发酵状态，无需复杂操作
- **状态追踪**：清晰了解每个发酵罐的当前状态
- **家谱管理**：追溯发酵项目的传代和分装关系
- **及时提醒**：不错过关键的检查和操作时机

### 1.4 MVP功能范围
专注于核心的"记录-追踪-提醒"闭环，去除复杂的分析和社交功能。

## 二、核心功能设计

### 2.1 发酵罐管理

#### 2.1.1 创建发酵罐

**页面布局：**
```
┌─────────────────────────────┐
│        创建发酵罐            │
├─────────────────────────────┤
│   罐名称：                   │
│   ┌─────────────────────┐   │
│   │ [文本输入框]         │   │
│   └─────────────────────┘   │
│                             │
│   发酵类型：                 │
│   [泡菜] [康普茶] [面团]      │
│   [果酒] [酸奶] [其他]        │
│                             │
│   封面照片：                 │
│   [📷 拍照] [🖼️ 相册]       │
│                             │
│   [创建] [取消]              │
└─────────────────────────────┘
```

**字段信息：**
- 罐名称（必填，20字符以内）
- 发酵类型（6个预设选项，支持自定义）
- 封面照片（选填，自动压缩至800x800）
- 创建时间（系统自动）

#### 2.1.2 发酵罐列表（首页）

**页面布局：**
```
┌─────────────────────────────┐
│    🧪 发酵笔记               │
│  [全部] [进行中] [已完成]     │
├─────────────────────────────┤
│  ┌─────────────────────┐   │
│  │ [图] 苹果鲁邦        │   │
│  │     面团 · 进行中     │   │
│  │     第5天 🟢         │   │
│  │     2小时前有记录     │   │
│  └─────────────────────┘   │
│  ┌─────────────────────┐   │
│  │ [图] 红茶康普茶       │   │
│  │     饮品 · 需关注     │   │
│  │     第12天 🟡        │   │
│  │     8小时前有记录     │   │
│  └─────────────────────┘   │
├─────────────────────────────┤
│            [➕]              │
└─────────────────────────────┘
```

**卡片信息优先级：**
- **主要信息**：罐名称、状态颜色、发酵天数
- **次要信息**：发酵类型、最后记录时间
- **交互元素**：状态指示器、快捷记录按钮

**状态颜色规范：**
- 🟢 正常进行（绿色）
- 🟡 需要关注（橙色）  
- 🔴 出现异常（红色）
- 🔵 已完成（蓝色）

#### 2.1.3 发酵罐详情

**页面结构：**
```
┌─────────────────────────────┐
│   ← 苹果鲁邦                 │
├─────────────────────────────┤
│        [封面图片]             │
│                             │
│   面团发酵 · 进行中 🟢        │
│   开始时间：9月18日           │
│   持续时间：5天              │
├─────────────────────────────┤
│   📝 记录时间线               │
│                             │
│   🕒 2小时前                 │
│   观察：体积翻倍，气泡均匀     │
│   状态：正常进行 🟢           │
│   [📷📷]                     │
│                             │
│   🕒 昨天 20:00              │
│   投料：面粉100g 水90g        │
│   状态：正常进行 🟢           │
│                             │
│   🕒 3天前                   │
│   观察：开始有酸香味          │
│   状态：正常进行 🟢           │
├─────────────────────────────┤
│   [📝 添加记录] [⏰ 设置提醒]  │
└─────────────────────────────┘
```

### 2.2 统一记录系统

#### 2.2.1 添加记录页面

**页面布局：**
```
┌─────────────────────────────┐
│   ← 添加记录                 │
├─────────────────────────────┤
│   记录时间：                 │
│   [9月23日 14:30] [现在]     │
│                             │
│   记录内容：                 │
│   ┌─────────────────────┐   │
│   │ 体积翻倍，有明显酸香... │   │
│   └─────────────────────┘   │
│                             │
│   状态标签：                 │
│   [🟢正常进行] [🟡需关注] [🔴异常] │
│                             │
│   快捷模板：                 │
│   [开始起泡] [颜色变化] [产生酸味] │
│   [需要搅拌] [准备分装] [其他]    │
│                             │
│   照片：                     │
│   [📷 拍照] [已添加: 📷📷]    │
│                             │
│   ▼ 投料记录（点击展开）       │
│                             │
│   [保存记录] [取消]           │
└─────────────────────────────┘
```

**核心功能：**

1. **基础记录**
   - 记录时间（默认当前，可调整）
   - 记录内容（文本框，300字以内）
   - 状态标签（3个选项，快速选择）
   - 照片（最多3张，自动压缩）

2. **快捷模板**（一键填入常用描述）
   - 开始起泡
   - 颜色变化  
   - 产生酸味
   - 需要搅拌
   - 准备分装
   - 其他

3. **投料模块**（可选展开）
   ```
   ┌─────────────────────┐
   │  ▲ 投料记录          │
   │                     │
   │  投料内容：          │
   │  ┌─────────────────┐ │
   │  │ 面粉 100g        │ │
   │  │ 水 90g          │ │
   │  │ 盐 5g           │ │
   │  └─────────────────┘ │
   │                     │
   │  [保存投料记录]      │
   └─────────────────────┘
   ```

#### 2.2.2 快速记录（从首页直接操作）

**交互方式：**
- 在首页发酵罐卡片上添加"📝"快捷按钮
- 点击后弹出精简记录浮层

**快速记录浮层：**
```
┌─────────────────────────────┐
│   苹果鲁邦 - 快速记录         │
├─────────────────────────────┤
│   状态：                     │
│   [🟢正常] [🟡关注] [🔴异常]   │
│                             │
│   快速描述：                 │
│   [开始起泡] [颜色变化]       │
│   [产生酸味] [需要搅拌]       │
│                             │
│   或输入：                   │
│   ┌─────────────────────┐   │
│   │ [简短描述...]        │   │
│   └─────────────────────┘   │
│                             │
│   [📷] [保存] [详细记录]      │
└─────────────────────────────┘
```

### 2.3 分装功能（MVP-简洁通用版）

目标：以最少字段覆盖“留种/使用去向/传代或二发”通用场景，步骤短、概念少、默认合理。

#### 2.3.1 表单与交互

**触发方式：**
- 详情页“更多”→“分装”，或首页卡片长按→“分装”。

**最小表单：**
```
┌─────────────────────────────┐
│   分装                      │
├─────────────────────────────┤
│   原罐处理： [继续发酵▼]      │
│                             │
│   留种（可选）：             │
│   数量 [ 100 ] g/ml          │
│   说明 [ 作为下一代/留种 ]     │
│                             │
│   新建项：数量 [ 2▼ ]         │
│   命名前缀 [ 原名称 ]          │
│   命名样式 [ 分装1/2/3▼ ]      │
│   标签（可选） [ 子项/二发 ]    │
│                             │
│   记录预览（自动生成）         │
│   - 留种100g → 成为下一代       │
│   - 新建 2 项 → 原名称-分装1/2  │
│                             │
│   [确认分装]  [取消]            │
└─────────────────────────────┘
```

说明：
- 不收集具体配料/风味，仅用“标签”字段作通用分类（如“二发”、“子项”）。
- 留种与新建项均为可选，但至少需存在其一。
- 命名与数量为唯一必填（除非仅留种）。

#### 2.3.2 规则与默认

- 原罐处理：默认“继续发酵”。
- 留种数量：提供 50/80/100 的快捷建议（可自填），MVP 不做总量校验。
- 新建项数量：1–3，默认 2。
- 命名：默认“原名称-分装1..N”，可改前缀；同一父罐下需唯一。
- 标签：可选；用于后续筛选/家谱显示（不绑定具体品类）。

#### 2.3.3 保存结果（数据与家谱）

保存后自动：
1. 在父罐添加一条“分离与去向”记录，摘要包含留种与新建项信息。
2. 批量创建子项，设置 `parent_id=父罐id`，名称按命名规则递增。
3. 在每个子项添加初始“来源记录”。
4. 家谱视图即时可见父→子的连接。

#### 2.3.4 容错与撤销

- 校验：命名重复禁止保存；“留种或新建项至少存在其一”。
- 撤销：支持“撤销本次分装”，将删除子项与自动生成记录（限最近一次）。

（注：所有“风味/配料/容量”等细节留待后续版本，以保持MVP简洁通用。）

### 2.4 家谱追踪

#### 2.4.1 家谱视图

**页面布局：**
```
┌─────────────────────────────┐
│   🌳 家谱                    │
├─────────────────────────────┤
│                             │
│   红茶康普茶 第1代            │
│        │                   │
│   ├── 分装1 (橙味)          │
│   ├── 分装2 (柠檬味)         │
│   └── 红茶康普茶 第2代        │
│            │               │
│       ├── 分装3 (原味)      │
│       └── 分装4 (姜味)       │
│                             │
│   苹果鲁邦 第3代             │
│        │                   │
│   ├── 苹果鲁邦 第4代         │
│   └── 分装面包              │
│                             │
└─────────────────────────────┘
```

**功能特点：**
- 简单的树状图展示
- 点击节点查看详情
- 状态颜色标识（绿/橙/红/蓝）
- 显示传代和分装关系

### 2.5 提醒系统

#### 2.5.1 设置提醒

**提醒设置页面：**
```
┌─────────────────────────────┐
│   设置提醒 - 苹果鲁邦         │
├─────────────────────────────┤
│   提醒时间：                 │
│   [明天 9:00] [修改]         │
│                             │
│   提醒内容：                 │
│   ┌─────────────────────┐   │
│   │ 检查发酵状态         │   │
│   └─────────────────────┘   │
│                             │
│   快捷设置：                 │
│   [6小时后] [12小时后]       │
│   [明天同时] [后天同时]       │
│                             │
│   重复：                     │
│   [仅一次] [每天] [每2天]     │
│                             │
│   [保存提醒] [取消]          │
└─────────────────────────────┘
```

**预设提醒模板：**
- 检查发酵状态
- 需要搅拌
- 准备分装
- 品尝测试

#### 2.5.2 提醒管理

**提醒列表：**
```
┌─────────────────────────────┐
│   ⏰ 我的提醒                │
├─────────────────────────────┤
│   今天 15:00                │
│   🟢 苹果鲁邦               │
│   检查发酵状态               │
│   [完成] [延后1小时]          │
│                             │
│   明天 9:00                 │
│   🟡 红茶康普茶             │
│   准备分装                   │
│   [修改] [关闭]              │
│                             │
│   后天 20:00                │
│   🟢 酸奶                   │
│   品尝测试                   │
│   [修改] [关闭]              │
└─────────────────────────────┘
```

## 三、界面设计规范

### 3.1 颜色系统

```
主色调：
- 主色：#4CAF50 (绿色)
- 辅助色：#FF9800 (橙色)

状态颜色：
- 正常：#4CAF50 (绿色)
- 关注：#FF9800 (橙色)  
- 异常：#F44336 (红色)
- 完成：#2196F3 (蓝色)

界面颜色：
- 背景：#F5F5F5 (浅灰)
- 卡片：#FFFFFF (白色)
- 主文字：#212121 (深灰)
- 辅助文字：#757575 (中灰)
```

### 3.2 字体规范

```
标题：18sp，粗体
副标题：16sp，中等
正文：14sp，常规
辅助文字：12sp，常规
按钮：14sp，中等
```

### 3.3 组件规范

**卡片设计：**
- 圆角：8dp
- 阴影：elevation 2dp
- 内边距：16dp
- 卡片间距：12dp

**按钮设计：**
- 主要按钮：圆角24dp，主色背景
- 次要按钮：圆角24dp，边框样式
- 最小触摸区域：48dp x 48dp

**图标系统：**
- 使用Material Design图标
- 大小：24dp（小图标）、32dp（大图标）
- 颜色与文字保持一致

## 四、交互流程

### 4.1 核心用户流程

**新用户首次使用：**
```
下载APP → 引导页（3页） → 创建第一个发酵罐 → 
添加首次记录 → 设置提醒 → 返回首页 → 完成
```

**日常记录流程：**
```
收到提醒通知 → 点击通知进入APP → 
快速记录浮层 → 选择状态和模板 → 保存 → 
设置下次提醒（可选）→ 完成
```

**分装流程：**
```
进入发酵罐详情 → 点击"更多"→"分装" → 
设置原罐处理方式 → 设置新罐数量和名称 → 
确认分装 → 自动创建新罐 → 完成
```

### 4.2 关键交互原则

1. **最多2步完成常用操作**
   - 快速记录：首页快捷按钮 → 保存
   - 状态更新：点击状态标签 → 自动保存

2. **渐进式信息披露**
   - 基础功能前置显示
   - 高级功能（投料）折叠隐藏

3. **容错性设计**
   - 所有编辑操作可取消
   - 删除操作需要二次确认

4. **反馈及时性**
   - 操作后立即显示结果
   - 使用Toast提示操作结果

## 五、技术实现规范

### 5.1 数据结构

**发酵罐表 (fermentation)**
```sql
CREATE TABLE fermentation (
    id INTEGER PRIMARY KEY,
    name TEXT NOT NULL,
    type TEXT,
    cover_image TEXT,
    status TEXT DEFAULT 'active',
    parent_id INTEGER,
    created_at TIMESTAMP,
    updated_at TIMESTAMP
);
```

**记录表 (record)**
```sql
CREATE TABLE record (
    id INTEGER PRIMARY KEY,
    fermentation_id INTEGER,
    content TEXT,
    status TEXT,
    images TEXT,
    is_feeding BOOLEAN DEFAULT 0,
    feeding_content TEXT,
    created_at TIMESTAMP,
    FOREIGN KEY (fermentation_id) REFERENCES fermentation(id)
);
```

**提醒表 (reminder)**
```sql
CREATE TABLE reminder (
    id INTEGER PRIMARY KEY,
    fermentation_id INTEGER,
    remind_time TIMESTAMP,
    content TEXT,
    repeat_type TEXT,
    is_active BOOLEAN DEFAULT 1,
    created_at TIMESTAMP,
    FOREIGN KEY (fermentation_id) REFERENCES fermentation(id)
);
```

### 5.2 存储规范

- 图片存储：应用私有目录，自动压缩至800x800
- 数据库：SQLite本地存储
- 图片路径：JSON数组格式存储
- 单次备份大小限制：50MB

### 5.3 性能要求

- 启动时间：<2秒
- 页面切换：<300ms
- 图片加载：<1秒
- 数据保存：<500ms

## 六、开发优先级

### Phase 1（核心功能）
- ✅ 发酵罐创建和列表
- ✅ 基础记录功能
- ✅ 状态管理
- ✅ 简单提醒

### Phase 2（增强体验）
- ✅ 快速记录浮层
- ✅ 投料模块
- ✅ 分装功能
- ✅ 家谱视图

### Phase 3（完善细节）
- ✅ 快捷模板优化
- ✅ 提醒管理
- ✅ 数据导出
- ✅ 界面优化

### 暂不实现
- ❌ AI功能
- ❌ 社交分享
- ❌ 复杂数据分析
- ❌ IoT集成
- ❌ 多设备同步

## 七、成功指标

### 用户体验指标
- 新用户完成首次记录率 > 80%
- 日均使用时长 > 3分钟
- 用户留存率（7天）> 60%

### 功能使用指标
- 快速记录使用比例 > 70%
- 提醒设置使用率 > 50%
- 分装功能使用率 > 30%

### 技术指标
- 应用崩溃率 < 1%
- 页面加载时间 < 500ms
- 存储空间使用 < 100MB

---

**总结：**
这个MVP版本专注于核心的"记录-追踪-提醒"功能闭环，去除了复杂的AI、社交、分析功能，确保用户能够简单高效地管理自己的发酵项目。设计原则是"简单易用，功能完整"，为后续版本的功能扩展留出空间。
